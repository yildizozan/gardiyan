apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gardiyan.fullname" . }}-test-connection"
  labels:
    {{- include "gardiyan.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gardiyan.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
